# Copy to .env.local for local development.
# In Railway, set this as a Variable on the frontend service instead.

# Base URL of the deployed backend API, e.g. https://smart-billing-api.up.railway.app/api/v1
VITE_API_BASE_URL=/api/v1
